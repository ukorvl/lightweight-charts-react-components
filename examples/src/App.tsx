export const App = () => {
  return (
    <div>
      <h1>React Chart</h1>
      <div>

      </div>
    </div>
  );
};

// a grid with small charts, each chart panel is interactve
// and allows to propagate into a particular chart page
// this pages will be used in the docs for an example
